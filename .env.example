# URL base del backend (memory-box-back). Debe incluir la barra final.
# En dev local: http://localhost:8000/
# Si se deja vacío o "/", el cliente usa el host actual del navegador en :8000.
NEXT_PUBLIC_API_URL=http://localhost:8000/
